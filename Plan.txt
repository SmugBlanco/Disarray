static dictictionary => type -> Class
list => class -> type, internal name
instanced dictionary => class -> has

this replaces my 'Electrified' thing

So this would work for buffs and i suppose variables if we cut out the static dictionary?


maybe instead we have an "Active List" that we can call 